@contacts.map do |contact|
{
  first_name: contact.first_name,
  last_name: contact.last_name,
  email: contact.email,
  phone_number: contact.phone_number,
  id: contact.id,
  middle_name: contact.middle_name, 
  bio: contact.bio_name
 }
end